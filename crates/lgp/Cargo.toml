[package]
name = "lgp"
description = "Auto pull/push the clean, in-sync repos in a folder, then show the overview"
version.workspace = true
edition.workspace = true
license.workspace = true

[dependencies]
grove-core = { path = "../grove-core" }
clap = { version = "4", features = ["derive"] }
clap_mangen = "0.2"
