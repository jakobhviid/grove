[package]
name = "gc"
description = "git commit; -a stages tracked changes, -p pushes after"
version.workspace = true
edition.workspace = true
license.workspace = true

[dependencies]
grove-core = { path = "../grove-core" }
clap = { version = "4", features = ["derive"] }
clap_mangen = "0.2"
