[package]
name = "grove"
description = "The grove command suite: friendly overview + shell-alias setup (grove init)."
version.workspace = true
edition.workspace = true
license.workspace = true

[dependencies]
grove-core = { path = "../grove-core" }
clap = { version = "4", features = ["derive"] }
clap_complete = "4"
clap_mangen = "0.2"
